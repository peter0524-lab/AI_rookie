# Public release policy

This public-safe repository excludes detailed logs, raw/private datasets, detailed PII prediction traces, prompt text examples, and files matching PII/API-key scanner patterns.

The complete internal backup remains local only under `backups/campfire_essential_backup_*.tar.zst`.
